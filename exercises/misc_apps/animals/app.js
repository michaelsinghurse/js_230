$(function() {
  // add listener on all the images
  // - hover for 2 seconds
  // - select the figcaption sibling
  // - toggle it's display property (current its display: none)
  // add listener for mouseleave
  // - if the figcaption is visible, turn off the display

});
